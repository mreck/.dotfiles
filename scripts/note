#!/bin/sh

(
	cd "$HOME/notes"

	case "$1" in
		grep | g )
			shift
			grep -nir --color=always -e "$1" .
			;;
		* )
			fname="$(fzf --print-query | tail -1)"
			if [ ! -z "$fname" ]; then
				vim "$fname"
			fi
			;;
	esac
)
